---
phase: 01.1-autonomous-skill-distillation-loop
plan: "01"
subsystem: openfang-types/config
tags: [config, distillation, requirements, tdd]
dependency_graph:
  requires: []
  provides: [DistillationConfig, KernelConfig.distillation, Phase-1.1-requirements]
  affects: [openfang-types, .planning/REQUIREMENTS.md]
tech_stack:
  added: []
  patterns: [serde-default-sub-struct, tdd-red-green]
key_files:
  created:
    - .planning/phases/01.1-autonomous-skill-distillation-loop/01.1-01-SUMMARY.md
  modified:
    - crates/openfang-types/src/config.rs
    - .planning/REQUIREMENTS.md
decisions:
  - DistillationConfig uses #[serde(default)] without deny_unknown_fields, per RESEARCH.md Pitfall 1 / X-01 requirement
  - DistillationConfig derives PartialEq to support assert_eq! in omitted-section test
  - Phase 1.1 X-01/X-02 IDs are distinct from Phase 1 X-01/X-02 (different prefix namespace per phase)
metrics:
  duration_seconds: 390
  completed_date: "2026-06-10"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 2
requirements: [X-01, X-02]
---

# Phase 1.1 Plan 01: DistillationConfig + Phase 1.1 Requirements SUMMARY

**One-liner:** `DistillationConfig` struct (6 opt-in tunables) wired into `KernelConfig` + `Default`, with all 13 Phase 1.1 REQ-IDs defined in `REQUIREMENTS.md`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add DistillationConfig struct and wire it into KernelConfig + Default | `e48dfca` | `crates/openfang-types/src/config.rs` |
| 2 | Author Phase 1.1 requirements section in REQUIREMENTS.md | `fca9136` | `.planning/REQUIREMENTS.md` |

## What Was Built

### Task 1: DistillationConfig

Added `pub struct DistillationConfig` to `crates/openfang-types/src/config.rs` immediately after `KernelCapabilities`:

- 6 fields: `enabled: bool`, `reflection_threshold: f32`, `daily_cap: u32`, `auto_approve_non_protected: bool`, `consolidation_nudge_hours: u64`, `failure_patch_threshold: u32`
- `#[serde(default)]` on the struct (NOT `deny_unknown_fields` — per X-01 / RESEARCH.md Pitfall 1)
- `#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]`
- `impl Default` with: `enabled=false`, `reflection_threshold=0.5`, `daily_cap=10`, `auto_approve_non_protected=false`, `consolidation_nudge_hours=0`, `failure_patch_threshold=3`
- `pub distillation: DistillationConfig` field added to `KernelConfig` struct after `capabilities`
- `distillation: DistillationConfig::default()` entry added to `impl Default for KernelConfig`

TDD workflow:
- RED commit `d8c0c52`: 3 failing tests (struct didn't exist)
- GREEN commit `e48dfca`: implementation — all 3 tests pass

### Task 2: Phase 1.1 Requirements

Appended `## Phase 1.1: Autonomous Skill Distillation Loop` to `.planning/REQUIREMENTS.md`:

- **SD-01..SD-06** — Skill Distillation subsystem
- **SI-01..SI-03** — Skill Self-Improvement subsystem
- **MC-01..MC-02** — Memory Consolidation Nudge subsystem
- **X-01..X-02** — Cross-cutting config requirements
- Goal-backward mapping table with 10 observable success criteria

All 13 IDs follow Phase 1 bold-ID prose format.

## Verification Results

```
cargo build -p openfang-types --lib    -> Finished (no errors)
cargo test -p openfang-types distillation -> 3 passed; 0 failed
cargo clippy -p openfang-types --all-targets -- -D warnings -> Finished (no warnings)
```

Acceptance criteria:
- `pub struct DistillationConfig` at line 746 of config.rs
- `pub distillation: DistillationConfig` at line 1394 of config.rs
- `distillation: DistillationConfig::default()` at line 1762 of config.rs
- No `deny_unknown_fields` on DistillationConfig
- All 13 Phase 1.1 IDs present in REQUIREMENTS.md
- `## Phase 1.1: Autonomous Skill Distillation Loop` heading present
- `### Mapping to Phase 1.1 success criteria` table present

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written, with one minor addition: `PartialEq` derive added to `DistillationConfig` (Rule 2 — required to satisfy `assert_eq!(d, DistillationConfig::default())` in the omitted-section test, which the plan specifies).

## Known Stubs

None. This plan delivers configuration primitives only — no data flows to UI or stubs any runtime behavior.

## Self-Check: PASSED

Files created/modified:
- FOUND: crates/openfang-types/src/config.rs (DistillationConfig at line 746)
- FOUND: .planning/REQUIREMENTS.md (Phase 1.1 section at line 113)
- FOUND: .planning/phases/01.1-autonomous-skill-distillation-loop/01.1-01-SUMMARY.md

Commits verified:
- d8c0c52 test(01.1-01): add failing tests for DistillationConfig
- e48dfca feat(01.1-01): DistillationConfig struct wired into KernelConfig + Default
- fca9136 feat(01.1-01): author Phase 1.1 requirements in REQUIREMENTS.md
